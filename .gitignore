# ── Gradle ──
.gradle/
**/build/
local.properties
/Focus Mode App/

# Wrapper JAR must stay tracked (do not blanket-ignore *.jar at repo root)
!gradle/wrapper/gradle-wrapper.jar

# ── Allow the debug APK through ──
!app/build/
app/build/*
!app/build/outputs/
app/build/outputs/*
!app/build/outputs/apk/
app/build/outputs/apk/*
!app/build/outputs/apk/debug/
app/build/outputs/apk/debug/*
!app/build/outputs/apk/debug/app-debug.apk

# ── Android / Kotlin ──
*.apk
*.aab
*.ap_
*.dex
*.class
bin/
gen/
out/
captures/
.externalNativeBuild/
.cxx/
*.hprof

# Re-allow the debug APK (overrides *.apk above)
!app/build/outputs/apk/debug/app-debug.apk

# ── IDE — Android Studio / IntelliJ ──
.idea/
*.iml
*.ipr
*.iws
.navigation/

# ── Keystores & secrets (never commit) ──
*.jks
*.keystore

# ── OS ──
.DS_Store
Thumbs.db
*.swp
*~

# ── Lint / analysis (regenerate locally) ──
lint-baseline.xml

# ── Fastlane / Play (if added later) ──
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# ── Kotlin compiler / tooling ──
.kotlin/

# Version catalog must remain tracked even if *.toml patterns are added later
!gradle/libs.versions.toml