# Backend Developer Conventions

- **Folder structure**: {e.g., feature-based vs layer-based}
- **API naming**: {e.g., RESTful -- plural nouns, /api/v1/users}
- **Response format**: {e.g., { data: ..., error: ..., meta: ... }}
- **Error handling**: {e.g., custom error classes, standard HTTP status codes}
- **Logging**: {e.g., structured JSON logs, log levels: debug/info/warn/error}
- **Authentication pattern**: {e.g., JWT in Authorization header, middleware-based}
- **Validation**: {e.g., validate at controller level, use schema validation library}
- **Service pattern**: {e.g., controller -> service -> repository}
